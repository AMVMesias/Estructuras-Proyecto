#include <iostream>
#include "Historial.h"
#include "Pedido.h"
#include "Lista.h"
#include  "Menu.h"
#include "Estudiante.h"
#include <windows.h>
using namespace std;

int main(){
SetConsoleOutputCP(CP_UTF8);
	Menu m;
	//	m.MenuLogin();
	//m.crearCuenta();
	//m.MostrarMenuPrincipal();
	
/*	 Estudiante student("John Doe", 12345, "password");

    // Create a new order for the student
    Pedido order;
    order.agregarProducto(Producto("Hamburgesa", 5.0, 1));
    order.agregarProducto(Producto("Papas Fritas", 3.0, 2));

    // Create a new receipt for the order
    Recibo receipt(1, order.calcularTotal(), student, order);

    // Add the receipt to the history
    Historial history;
    history.agregarRecibo(receipt);

    // Display the history
    history.mostrarHistorial();
*/
    return 0;
}
   
	
